/**
 * Craftdesk 方野 — PI-Desktop theme plugin.
 *
 * Theme CSS is declared in manifest.json (contributes.themes).
 * Lifecycle hooks stay as no-ops.
 */
async function onLoad() {}
async function onUnload() {}
module.exports = { onLoad, onUnload };
